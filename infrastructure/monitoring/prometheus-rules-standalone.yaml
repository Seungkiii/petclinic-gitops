apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: petclinic-scaling-alerts
  namespace: monitoring
  labels:
    release: my-monitoring
spec:
  groups:
    - name: hpa-scaling
      interval: 30s
      rules:
        - alert: HPAScalingActivity
          expr: |
            abs(
              kube_horizontalpodautoscaler_status_current_replicas
              - 
              kube_horizontalpodautoscaler_status_desired_replicas
            ) > 0
          for: 10s
          labels:
            severity: info
            component: hpa
          annotations:
            summary: '⚡ HPA is scaling {{ $labels.horizontalpodautoscaler }}'
            description: 'Current: {{ $value }} replicas. Adjusting to meet demand.'
        
        - alert: HPAMaxReplicasReached
          expr: |
            kube_horizontalpodautoscaler_status_current_replicas 
            >= 
            kube_horizontalpodautoscaler_spec_max_replicas
          for: 30s
          labels:
            severity: warning
            component: hpa
          annotations:
            summary: '⚠️ HPA reached max replicas'
            description: 'HPA {{ $labels.horizontalpodautoscaler }} reached maximum: {{ $value }} replicas. Consider increasing limits.'
        
        - alert: HighCPUUtilization
          expr: |
            avg(rate(container_cpu_usage_seconds_total{namespace="default",container!=""}[5m])) by (pod) 
            / 
            avg(kube_pod_container_resource_requests{resource="cpu", namespace="default"}) by (pod) 
            > 0.8
          for: 2m
          labels:
            severity: warning
            component: pod
          annotations:
            summary: '🔥 {{ $labels.pod }} has high CPU'
            description: 'CPU usage is above 80% of request'
    
    - name: karpenter-scaling
      interval: 30s
      rules:
        - alert: KarpenterNodeCreated
          expr: |
            increase(karpenter_nodeclaims_created_total[2m]) > 0
          labels:
            severity: info
            component: karpenter
          annotations:
            summary: '🚀 Karpenter created new node(s)'
            description: 'NodePool created new node(s)'
        
        - alert: KarpenterNodeTerminated
          expr: |
            increase(karpenter_nodeclaims_terminated_total[2m]) > 0
          labels:
            severity: info
            component: karpenter
          annotations:
            summary: '✅ Karpenter terminated node(s)'
            description: 'NodePool terminated node(s) to save cost'
        
        - alert: KarpenterHighPendingPods
          expr: |
            sum(kube_pod_status_phase{phase="Pending", namespace="default"}) > 5
          for: 3m
          labels:
            severity: warning
            component: karpenter
          annotations:
            summary: '⏳ Multiple pods pending scheduling'
            description: 'Multiple pods pending for >3 minutes. Karpenter may need to provision nodes.'
